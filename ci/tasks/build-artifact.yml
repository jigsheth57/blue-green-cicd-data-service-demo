---
platform: linux

image_resource:
  type: docker-image
  source: {repository: jigsheth57/pcfworkshop}

inputs:
  - name: lab-repo
  - name: version

outputs:
  - name: build-artifact

run:
  path: lab-repo/lab1/ci/tasks/build-artifact.sh
